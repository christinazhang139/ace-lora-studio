apiVersion: v1
kind: Service
metadata:
  name: ace-studio
  labels:
    app: ace-studio
spec:
  selector:
    app: ace-studio
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
