apiVersion: v1
kind: Service
metadata:
  name: ace-lora-studio
  labels:
    app: ace-lora-studio
spec:
  selector:
    app: ace-lora-studio
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
