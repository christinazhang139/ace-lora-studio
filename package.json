{
  "private": true,
  "workspaces": ["packages/ui", "studio"],
  "scripts": {
    "build": "npm run build --workspace=studio",
    "start": "npm start --workspace=studio",
    "dev": "npm run dev --workspace=studio"
  }
}
